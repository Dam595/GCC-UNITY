using UnityEngine;

public class GameObk : MonoBehaviour
{
    private void Awake()
    {
        
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    private void OnEnable()
    {
        
    }
    /// <summary>
    /// 
    /// </summary>
    // Update is called once per frame
    void Update()
    {
        
    }
}
